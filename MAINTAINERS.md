# 维护说明

## 内容分层

- `docs/cards/`保存稳定的管理方法。只有课程框架或关键定义改变时才修改。
- `docs/frontier/`保存会变化的模型、产品和协议事实，每张事实卡必须有观察日期、适用范围和官方入口。
- `docs/cases/`明确标注真实公开案例或课程合成情境，不能混写。
- `.agents/skills/`负责学习行为，不复制整套知识内容。

## 动态事实复核

课前或公开发布前检查：页面是否仍存在、发布日期与更新日期、正式/预览状态、可用产品与地区、能力是否依赖特定工具环境。厂商评测不直接转写成企业ROI或上线结论。

## 修改检查

1. 所有相对链接能够从所在文件打开。
2. `card_id`唯一，知识导航能找到新增卡片。
3. Skill名称与目录一致，描述能够区分学习辅导和外部业务执行。
4. OpenCode能发现Skill和五个命令；项目配置保持只读，联网需询问，会话分享关闭。
5. 搜索临时课程编号、学号、人名和其他学员资料，确认未进入知识库。
6. 对外发布前单独决定许可证、仓库可见性和发布位置。

## 网页发布与视觉约束

- 学员网站以白底、深蓝、灰色为主，舒展行距；不用角色多色编码、大面积渐变、装饰轨道或叠放卡片。
- 知识卡、角色路径、案例、模板、指南和来源页必须站内阅读；GitHub只用于源码、下载和维护，不作为正文阅读入口。
- Markdown是正文的唯一维护源；`scripts/build-site.mjs`生成首页、WEF专题及`pages/`阅读页。不要手改生成后的HTML正文。
- `data/wef-graph.json`保存节点、关系、证据位置和管理问题，网页图谱和证据明细由同一数据生成。
- 本地安装：`npm ci`；生成：`npm run build`；检查：`npm run check`。将源稿与生成结果一起提交到main，GitHub Pages从根目录发布。
- 修改后检查首页、正文和图谱的桌面与手机效果，验证三个问题切换、关系选择、来源页码及站内链接。
- 图谱每条关系必须有稳定ID、主体、谓词、客体、来源、页码、适用边界和核验日期；报告主张与课程解释分开，不能把广义“业务行动”直接合并为“AI自主执行”。
- 更新报告时重新检查官方PDF页码，保留原文链接；不把两份不同样本报告的数字拼成趋势。
